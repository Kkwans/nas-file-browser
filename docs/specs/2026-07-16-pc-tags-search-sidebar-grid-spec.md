# PC 端标签、搜索、侧边栏与详细网格优化规格

> 状态：实施中
>
> 基线：`master` / `82bba2f`
>
> 日期：2026-07-16
>
> 范围：PC 端标签、搜索/标签筛选结果页、侧边栏和详细网格；不包含移动端专项、部署或运行时变更

## 1. 目标与执行约束

本轮以用户 2026-07-16 提供的 7 张 PC 端标注图和逐条清单为唯一需求基线。目标不是继续追加局部 CSS 覆盖，而是清理重复规则、建立可复用组件和稳定的数据/交互契约。

- 所有用户要求都映射为唯一任务编号，并在本文件持续更新状态。
- 每个行为变更先写失败测试，再做最小实现，完成后运行聚焦测试、类型检查和构建。
- 一个提交只处理一个模块，使用中文 Conventional Commits。
- 延续现有浅色、克制、内容密集型文件管理器语言；不引入新主题、新字体、渐变或夸张阴影。
- 复用现有 Material Icons 和语义色令牌；交互态不得造成尺寸或位置抖动。
- Windows 只修改同步源码；真实 Git 提交和推送在 NAS 仓库核验执行。本轮不部署。

## 2. 统一视觉与交互契约

### 2.1 视觉层级

- 一级模块：用户、收藏夹、标签、存储卷、目录分类。统一 44px 标题行、12px 圆角、浅色模块表面和 1px 边界。
- 二级分组：收藏分组、目录分类分组。统一 40px 行高、10px 圆角、较一级更弱的表面层级。
- 三级条目：收藏路径、标签、存储卷、分类路径。统一图标列、主信息列、计数/动作列。
- hover 只使用比静态卡片更浅的状态层，不改变宽度、高度或圆角，也不超出父卡片边界。
- 图标与文字采用固定列对齐；可点击图标保留至少 36×36px PC 命中区。

### 2.2 间距与形态

- 使用 `4 / 8 / 12 / 16 / 24px` 间距体系。
- 普通卡片圆角 10–12px，输入框与按钮 8px；颜色点保持正圆。
- 同层级计数徽标统一高度、最小宽度、数字字体和背景，不按模块各自实现。
- 删除类操作必须二次确认；确认信息靠近触发对象，不放到无关列表末尾。

## 3. 模块任务清单

### A. 标签模块

| ID | 要求 | 技术方案 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| TAG-01 | 重做颜色选中态 | 颜色本体不加灰黑内圈；使用白色间隔环 + 2px 主色外环和勾选标记，尺寸不变化 | 选中前后圆点位置/尺寸不变，焦点和选中清晰且不显厚重 | 待处理 |
| TAG-02 | 重做自定义颜色 | 使用完整彩色圆盘，中心使用同一图标库的取色图标，不再出现外圆内方 | 外观为单一正圆，与预设色尺寸、基线一致 | 待处理 |
| TAG-03 | 重排创建区 | 首行采用“名称输入框 + 创建按钮”；颜色网格单独占满整行 | 创建按钮不再单独占行；两行颜色左右对称、无尾部大空白 | 待处理 |
| TAG-04 | 重排编辑态 | 编辑行拆成名称、紧凑颜色区和固定动作区；颜色不重叠 | 36rem 弹窗内所有颜色完整可见、不互相覆盖 | 待处理 |
| TAG-05 | 优化预设色 | 调整近似蓝色，使用 18 个高区分度颜色组成 9×2 网格（最后一格为自定义颜色入口） | 相邻颜色肉眼可区分，两行等宽对齐 | 待处理 |
| TAG-06 | 颜色唯一性 | 已被其他标签使用的预设色显示禁用；创建/编辑时阻止重复颜色，自定义色重复也校验 | 同一用户不能保存两个相同颜色标签；编辑自身颜色不被误禁用 | 待处理 |
| TAG-07 | 详细网格只显示标签色 | 详细网格标记区只渲染颜色点，并以 title/aria-label 提供名称 | 多标签和长标签名不撑高卡片；详细列表仍显示名称 | 待处理 |
| TAG-08 | 删除确认靠近目标 | 在目标标签行下方展开内联确认区，不再渲染在列表末尾 | 确认内容与待删标签相邻；取消和删除均可键盘操作 | 待处理 |

### B. 搜索与标签筛选结果页

| ID | 要求 | 技术方案 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| SEARCH-01 | 主界面快速搜索居中 | 恢复 HeaderBar 中心布局，适度加宽，保持与右侧动作平衡 | PC 顶栏搜索框视觉居中，非输入态和聚焦态边界稳定 | 待处理 |
| SEARCH-02 | 重构 `/search` 输入区 | 搜索页复用主界面搜索框组件；返回入口移到结果页头卡片 | 输入框样式、宽度和交互与快速搜索一致；结果数不挤压输入文本 | 待处理 |
| SEARCH-03 | 共用结果页壳层 | 抽取 `UnifiedResultPage`/`UnifiedResultList`，搜索和标签只注入图标、标题、作用域与数据 | 两种页面头卡片、范围切换、返回按钮、列表卡片完全一致 | 待处理 |
| SEARCH-04 | 优化结果信息层级 | 名称与父路径采用不同字重/颜色，间距 4–6px；元信息固定在右侧 | 名称、路径和时间层级清晰，长内容省略但可查看完整 title | 待处理 |
| SEARCH-05 | 只显示父路径 | 增加路径工具函数，从完整结果路径剥离当前文件名并生成相对/可读父路径 | `V/1 (10).mp4` 显示为 `V/`，不重复文件名 | 待处理 |
| SEARCH-06 | 结果右键菜单 | 抽取结果动作模型，提供打开所在位置、移动、复制、下载、详细信息 | 搜索和标签结果均可右键；动作权限与文件列表一致 | 待处理 |
| SEARCH-07 | 打开所在位置 | 计算父目录路由并设置 `fileStore.preselect`，跳转后定位目标 | 文件结果跳到父目录并选中；目录结果跳到其父目录并选中 | 待处理 |
| SEARCH-08 | 默认当前目录 | 标签筛选入口改为 `scope=current`；切换范围保留 base/tag/query | 搜索和标签筛选默认当前目录，用户可切全局且不丢状态 | 待处理 |

### C. 侧边栏模块

| ID | 要求 | 技术方案 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| SIDE-01 | 主菜单图标左对齐 | 统一导航内容网格和左内边距 | 我的文件、搜索、新建、设置、登出与模块图标同一垂直线 | 待处理 |
| SIDE-02 | 搜索图标垂直居中 | 统一 `.action` 的行高和 `align-items`，清理重复覆盖 | 搜索图标与文字光学居中 | 待处理 |
| SIDE-03 | 清空收藏二次确认 | 新增就地确认气泡/弹层，确认前不执行删除 | 首次点击不删除；明确显示数量和不可撤销提示 | 待处理 |
| SIDE-04 | 放大模块动作 | 一级模块动作视觉尺寸 20px、命中区 36px | 新建分组、清空、标签管理易点击且基线一致 | 待处理 |
| SIDE-05 | 三层复用组件 | 抽取一级模块头、二级分组头和三级条目的统一类/组件契约 | 收藏、标签、存储卷、目录分类不再各自复制布局 | 待处理 |
| SIDE-06 | 区分静态卡片与 hover | 用状态层颜色而非同一背景；hover 不超出父宽 | 悬停与默认分组卡片明显但克制地区分 | 待处理 |
| SIDE-07 | 收藏拖动排序 | 扩展收藏 Store/API 顺序字段或复用现有排序能力；分组内/未分组均支持上下排序 | 拖动不仅能换分组，也能改变同级顺序；刷新后顺序保留 | 待处理 |
| SIDE-08 | 默认展开策略 | 收藏夹一级默认展开；收藏分组默认折叠；标签默认折叠，用户手动状态可持久化 | 首次访问符合默认；已有用户状态不被无条件覆盖 | 待处理 |
| SIDE-09 | 修正箭头旋转 | 箭头使用固定 36px 容器并绕中心旋转 | 展开/折叠中心点不位移 | 待处理 |
| SIDE-10 | 统一分类计数徽标 | 二级分组复用同一 count badge | 目录分类与收藏分组计数宽高、字体、背景一致 | 待处理 |
| SIDE-11 | 校准目录分类 | 核对后端分类 API、绿联共享目录语义和当前卷子目录来源；共享目录只显示真正共享入口 | 与绿联共享文件夹语义一致；无误分个人/系统路径 | 待处理 |
| SIDE-12 | 移除磁盘使用区 | 删除底部 usage 进度模块及对应请求/状态；存储卷单位改为十进制 B/KB/MB/GB/TB | 侧边栏不重复显示磁盘使用；不出现 MiB/GiB/TiB | 待处理 |
| SIDE-13 | 更新仓库链接 | credits 链接改为 `Kkwans/nas-file-browser`，产品名显示 `NAS File Browser` | 点击打开正确 GitHub 仓库，不再显示 upstream untracked | 待处理 |

### D. 详细网格

| ID | 要求 | 技术方案 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- |
| GRID-01 | 消除文件夹顶部空白 | 将打标/收藏定位到图标同一视觉行两侧，不单占工具行；图标区高度固定 | 文件夹图标顶部不再有整行空白，按钮不与图标/风险点冲突 | 待处理 |
| GRID-02 | 文件夹不保留大小空行 | 文件夹信息模板不渲染 size 槽位；文件继续显示大小 | 文件夹名称与修改时间节奏紧凑，无隐形占位 | 待处理 |
| GRID-03 | 有无标签保持基线 | 标记区设置固定最小高度；详细网格标签改为纯色点并限制单行 | 同一行卡片的文件类型、大小、修改时间水平对齐 | 待处理 |

## 4. 数据与组件设计

### 4.1 标签颜色状态

新增纯函数：

- `normalizeTagColor(color)`：统一十六进制大小写。
- `getUsedTagColors(tags, excludingId?)`：计算禁用色集合。
- `isTagColorAvailable(tags, color, excludingId?)`：创建/编辑校验。

UI 禁用只是反馈层，Store 的 `createTag` / `updateTag` 也必须拒绝重复颜色，避免绕过界面。失败应返回可识别结果，由弹窗显示中文就地提示。

### 4.2 共用结果模型

```ts
interface UnifiedResultItem {
  key: string;
  name: string;
  fullPath: string;
  parentPath: string;
  url: string;
  isDir: boolean;
  size?: number;
  modified?: string;
}
```

搜索 API 和标签路径分别适配到该模型；展示组件不关心数据来源。右键菜单只接收模型和权限，不直接依赖搜索/标签页面内部状态。

### 4.3 侧边栏层级组件

```text
SidebarPrimaryNavigation
SidebarUserCard
SidebarSection        # 一级模块
SidebarGroup          # 二级分组
SidebarItem           # 三级条目
SidebarCountBadge
SidebarConfirmPopover
```

先建立视觉/交互契约，再逐模块迁移；迁移完成后删除 `sidebar.css` 中重复定义同一选择器的历史覆盖块。

## 5. 测试与验证矩阵

### 5.1 自动测试

- 标签：颜色规范化、重复颜色拒绝、编辑自身颜色允许、颜色禁用集合。
- 结果路径：中文/编码路径、文件与目录父路径、根目录、尾部斜杠。
- 结果动作：打开所在位置路由、预选路径、权限动作集合。
- 侧边栏：默认折叠状态迁移、收藏排序、单位格式、共享目录分类。
- 详细网格：信息契约函数确保目录无 size、详细网格标签仅输出颜色标记。

### 5.2 静态与构建验证

每个模块至少运行：

```text
pnpm test -- <聚焦测试>
pnpm typecheck
```

最终运行：

```text
pnpm test
pnpm lint
pnpm typecheck
pnpm build
```

### 5.3 视觉验收（不在本轮自动操作浏览器）

- 以用户 7 张标注图的相同 PC 宽度复核每个红框问题。
- 标签弹窗：创建态、编辑态、删除确认、已用颜色禁用。
- 搜索/标签：当前目录/全局、文件/目录、中文长路径、右键菜单。
- 侧边栏：默认展开态、所有层级 hover、拖动排序、空分组、长名称。
- 详细网格：文件夹/文件、有标签/无标签、风险目录、选中态。

## 6. 提交计划与进度日志

| 顺序 | 提交 | 包含任务 | 状态 |
| --- | --- | --- | --- |
| 1 | `docs(spec): 记录 PC 端模块优化方案` | 本规格 | 已完成 |
| 2 | `refactor(tags): 重构标签颜色与管理交互` | TAG-01～TAG-08、GRID-03 标签部分 | 待处理 |
| 3 | `refactor(search): 统一搜索与标签筛选结果页` | SEARCH-01～SEARCH-08 | 待处理 |
| 4 | `refactor(sidebar): 重建侧边栏层级与收藏交互` | SIDE-01～SIDE-13 | 待处理 |
| 5 | `fix(grid): 优化详细网格信息节奏` | GRID-01～GRID-03 | 待处理 |

### 进度记录

- 2026-07-16：完成需求编号化、设计约束、组件方案、测试矩阵和提交拆分。
